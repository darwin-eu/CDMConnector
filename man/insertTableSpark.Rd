% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dbSource.R
\name{insertTableSpark}
\alias{insertTableSpark}
\title{Fast bulk insert of a local table on Spark / Databricks}
\usage{
insertTableSpark(cdm, name, table, overwrite = TRUE, batchSize = 5000L)
}
\arguments{
\item{cdm}{A \code{cdm_reference} or \code{db_cdm} source object backed by a
Spark/Databricks connection. Must have a \code{writeSchema}.}

\item{name}{Name of the destination table (single character).}

\item{table}{A local data frame to upload.}

\item{overwrite}{If \code{TRUE} (default), drop the table first if it exists.}

\item{batchSize}{Number of rows per \code{INSERT} statement. Default 5000.
Larger batches reduce round trips but Spark imposes a query-size
limit (~16MB) — reduce if you hit "query too large" errors with very
wide tables.}
}
\value{
A \code{cdm_table} referencing the newly inserted table.
}
\description{
Uploads a local data frame to Spark/Databricks using multi-row
\verb{INSERT INTO ... VALUES (...), (...), ...} statements. This is the same
mechanism \code{\link[=insertTable]{insertTable()}} now uses by default on Spark, so you only need
\code{insertTableSpark()} directly when you want to tune \code{batchSize}. Multi-row
VALUES inserts are dramatically faster than the \verb{INSERT ... SELECT ... UNION ALL} approach Spark's planner struggles with (benchmarked ~50x faster
at 1000 rows).
}
\details{
Only intended for Spark connections. For other dialects use
\code{\link[=insertTable]{insertTable()}}.
}
