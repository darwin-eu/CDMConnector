# Build Dose Era SQL

Build Dose Era SQL

## Usage

``` r
build_dose_era_sql(criteria, cdm_schema = "@cdm_database_schema")
```

## Arguments

- criteria:

  Criteria list.

- cdm_schema:

  CDM schema name.
