# Build Payer Plan Period SQL (simplified)

Build Payer Plan Period SQL (simplified)

## Usage

``` r
build_payer_plan_period_sql(criteria, cdm_schema = "@cdm_database_schema")
```

## Arguments

- criteria:

  Criteria list.

- cdm_schema:

  CDM schema name.
