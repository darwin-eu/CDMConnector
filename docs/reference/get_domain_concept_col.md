# Get domain concept column for criteria type (for COUNT DISTINCT)

Get domain concept column for criteria type (for COUNT DISTINCT)

## Usage

``` r
get_domain_concept_col(criteria)
```

## Arguments

- criteria:

  Criteria item (list).
