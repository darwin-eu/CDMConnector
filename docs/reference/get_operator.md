# Get SQL operator from op string

Get SQL operator from op string

## Usage

``` r
get_operator(op)
```

## Arguments

- op:

  Operator name (lt, lte, eq, gt, gte, etc.)

## Value

SQL operator string
