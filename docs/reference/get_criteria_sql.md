# Dispatch to appropriate criteria SQL builder

Dispatch to appropriate criteria SQL builder

## Usage

``` r
get_criteria_sql(criteria, cdm_schema = "@cdm_database_schema")
```

## Arguments

- criteria:

  List - criteria object (may be wrapped in type key)

- cdm_schema:

  CDM schema name

## Value

SQL string
