# Build Visit Occurrence SQL

Build Visit Occurrence SQL

## Usage

``` r
build_visit_occurrence_sql(criteria, cdm_schema = "@cdm_database_schema")
```

## Arguments

- criteria:

  Criteria list.

- cdm_schema:

  CDM schema name.
