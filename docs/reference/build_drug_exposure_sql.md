# Build Drug Exposure SQL

Build Drug Exposure SQL

## Usage

``` r
build_drug_exposure_sql(criteria, cdm_schema = "@cdm_database_schema")
```

## Arguments

- criteria:

  Criteria list.

- cdm_schema:

  CDM schema name.
